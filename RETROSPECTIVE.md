# Daily Device 회고

프로젝트를 진행하며 고민했던 구조, 데이터 패칭, 렌더링 전략에 대한 기록입니다.

## FSD 레이어 경계

FSD 구조를 적용하면서 `entities`와 `features`의 경계가 가장 모호했습니다.

여러 화면에서 공유되는 도메인 타입, API, query key, mutation hook은 `entities`에 두고, 상품 상세나 체크아웃처럼 사용자 액션과 화면 상태가 결합된 흐름은 `features`로 분리했습니다.

다만 장바구니와 찜하기처럼 mutation 자체가 액션 UI와 강하게 묶인 기능은 경계가 특히 모호했습니다. 이 경우 mutation과 캐시 갱신 로직은 `entities`에 두고, 버튼 클릭 흐름과 화면별 조합은 `features`에서 가져다 쓰는 방식으로 정리했습니다.

이 기준을 세운 뒤에는 공유 도메인 로직과 화면별 사용자 흐름을 분리하기 수월해졌고, 특정 페이지에서만 필요한 UI 상태가 도메인 레이어에 섞이는 것을 줄일 수 있었습니다.

## React Context에서 Zustand로 전환

초기에는 장바구니, 체크아웃, 상품 필터, 마이페이지 shell, Hero와 네비게이션 문구 색상처럼 여러 컴포넌트가 공유하는 클라이언트 상태를 기능별 React Context로 관리했습니다. 기능이 늘면서 Provider의 위치와 생명주기를 함께 추적해야 했고, 하나의 Context에 포함된 여러 상태 중 일부만 필요한 컴포넌트도 Context 값 변경의 영향을 받기 쉬웠습니다.

이를 Zustand store로 이전하고 FSD slice 가까이에 배치했습니다. 컴포넌트는 필요한 상태와 action만 selector로 구독하게 했고, 서버에서 가져오는 데이터는 계속 TanStack Query가 담당하도록 역할을 나눴습니다. 여러 컴포넌트가 공유할 필요가 없는 짧은 UI 상태는 로컬 `useState`에 남겨, 모든 상태를 전역 store로 옮기지는 않았습니다.

상태가 가장 복잡한 장바구니는 비회원 장바구니, drawer 표시 여부, 상품별 수량, mutation 진행 상태를 각각의 store로 분리했습니다. 비회원 장바구니에는 `persist` middleware를 적용하되, 기존 Context 구현이 저장한 배열 형식도 복원할 수 있는 storage adapter를 두었습니다. 또한 `skipHydration`과 별도의 동기화 컴포넌트로 브라우저 저장소 복원 시점을 제어해 SSR 결과와 클라이언트 초기 상태가 어긋나지 않도록 했습니다.

그 결과 공통 Provider 구성이 단순해졌고, store의 상태 전이도 React 트리를 구성하지 않고 독립적으로 검증할 수 있게 됐습니다. 상태 관리 도구를 선택할 때는 단순히 공유 여부만 보는 것이 아니라 상태의 소유 범위, 유지 기간, 구독 단위와 서버 상태인지 여부를 함께 구분해야 한다는 기준을 세웠습니다.

## TanStack Query 캐시 무효화와 낙관적 업데이트

mutation 성공 후 어떤 query를 무효화할지 기준이 필요했습니다.

장바구니, 찜하기, 배송지, 상품평처럼 여러 화면의 데이터가 함께 바뀌는 경우 관련 query key를 명확히 분리하고, 성공 시점에 `invalidateQueries`로 캐시를 서버 상태와 맞췄습니다.

장바구니 수량 조절·삭제와 찜하기는 여기에 낙관적 업데이트를 더해 응답성을 유지했습니다. 낙관적 업데이트가 실패할 경우를 대비해 `onMutate`에서 변경 전 캐시를 스냅샷으로 저장하고, `onError`에서 롤백하는 방식으로 처리했습니다.

또한 빠른 연속 클릭이나 진행 중인 백그라운드 refetch와의 경합을 줄이기 위해, 스냅샷을 저장하기 전 `cancelQueries`로 해당 query의 진행 중인 요청을 먼저 취소하도록 했습니다.

## `useSuspenseQuery`와 `useQuery` 사용 기준

서버 페이지네이션이 있는 주문 목록에 `useSuspenseQuery`를 쓰자, 캐시되지 않은 페이지로 이동할 때 Suspense fallback이 발생하며 컴포넌트가 재마운트되고 페이지 상태가 초기화되는 문제가 있었습니다.

반면 전체 데이터를 한 번에 가져와 클라이언트에서 slice 하는 배송지 화면은 query key가 바뀌지 않아 같은 문제가 발생하지 않았습니다.

이후 마운트 시 반드시 필요한 초기 데이터나 독립적인 섹션에는 `useSuspenseQuery`를 사용하고, 페이지네이션/정렬/필터처럼 사용자 조작으로 요청 파라미터가 바뀌는 화면에는 `useQuery`와 `placeholderData`를 사용하는 기준을 세웠습니다.

이를 통해 페이지 이동 중에도 기존 화면을 유지하면서 부분 로딩 상태를 표시할 수 있었고, Suspense fallback으로 인한 상태 초기화를 줄일 수 있었습니다.

## 상품 상세 페이지의 prefetch와 hydration

상품 상세 페이지에서는 서버에서 필요한 데이터를 미리 가져온 뒤, TanStack Query의 `HydrationBoundary`를 통해 클라이언트 캐시에 전달했습니다.

이를 통해 클라이언트 컴포넌트에서도 같은 query key로 상품 정보, 이미지, 상품평, 추천 상품 데이터를 재사용할 수 있었고, 초기 진입 후 불필요한 중복 요청을 줄일 수 있었습니다.

또한 찜 여부나 장바구니 상태처럼 사용자에 따라 달라지는 데이터는 클라이언트에서 별도로 조회하도록 분리했습니다.

## 정적/동적 렌더링 경계와 `auth()` 위치

처음에는 공통 쇼핑 레이아웃에서 장바구니와 위시리스트 데이터를 함께 준비하려 했지만, 이 과정에서 `auth()` 호출이 공개 상품 페이지의 정적 최적화를 막는다는 점을 확인했습니다.

`auth()`를 레이아웃에서 제거하고 로그인 상태가 필요한 부분만 `useSession()`을 사용하는 클라이언트 컴포넌트로 분리하자, 상품/카테고리 페이지가 정적 또는 ISR로 전환됐습니다.

반면 체크아웃, 마이페이지, 관리자 페이지, API Route처럼 사용자별 권한과 요청 시점의 데이터가 핵심인 영역은 그대로 동적으로 처리했습니다.

이 경험을 통해 한 페이지 안에서도 사용자와 무관한 공통 데이터와 로그인 사용자별 상태를 분리하는 것이 중요하다는 점을 체감했습니다.

## GitHub Actions 정적 생성 병목 개선

Playwright E2E에서 production build를 실행할 때 전체 정적 생성 페이지가 391개까지 늘어났고, 그중 다수를 차지하는 상품과 카테고리 경로가 원격 TiDB를 반복해서 조회하면서 전체 작업에 약 12분이 걸렸습니다. DB 연결 안정성을 위해 정적 생성 동시성을 낮춘 상태라 timeout을 늘리거나 병렬성을 무리하게 높이는 방식만으로는 해결하기 어려웠습니다.

GitHub Actions의 `E2E_BUILD` 환경에서는 E2E가 실제로 사용하는 대표 카테고리와 상품 하나만 사전 생성하고, 로컬 및 Vercel build는 기존처럼 전체 경로를 생성하도록 분리했습니다. 대표 경로는 E2E DB에서 테스트에 필요한 이미지, 기본 색상과 번역이 준비된 첫 번째 상품과 해당 카테고리를 조회해 결정하고, Playwright도 같은 기준을 사용합니다. 그 결과 E2E build의 정적 생성 페이지가 65개로 줄었고, 최적화 직후 전체 workflow 시간은 약 12분에서 3분대로 단축됐습니다. 이후 실행 시간은 원격 DB와 네트워크 상태에 따라 대체로 3~5분 사이에서 변동했습니다.

후속 실행에서는 build에 적용한 DB 연결 옵션이 Playwright runtime에는 전달되지 않아 Auth.js 세션과 상품평 조회에서 `P1001`, `P2024`가 발생하고, 첫 테스트 시도가 실패한 뒤 재시도로 통과하는 `flaky` 상태도 확인했습니다. E2E DB URL 검증과 연결 옵션 구성을 공용 함수로 모으고, DB 준비와 Playwright의 직접 연결에 `connection_limit=3`, `connect_timeout=30`, `pool_timeout=60`을 동일하게 적용했습니다. 기존 SSL 옵션은 유지하고 중복된 연결 옵션은 교체하도록 한 뒤 전체 Playwright 테스트가 재시도 없이 통과하는 것을 확인했습니다.

이 과정에서 캐시나 timeout을 먼저 조정하기보다 각 단계의 실행 시간과 성공한 workflow 안의 `flaky` 로그까지 확인하고, CI의 검증 목적을 유지하는 범위에서 불필요한 작업량과 연결 불안정 원인을 함께 줄이는 것이 더 효과적이라는 점을 확인했습니다.

## React Hook Form과 Zod 기반 폼 상태 관리

처음에는 배송지와 상품평, 관리자 콘텐츠 폼의 값을 `useState`와 기능별 custom hook으로 관리하고, 정규표현식과 유틸 함수로 입력값을 검증했습니다. 필드와 동적 배열이 늘어나자 값 변경 handler와 오류 상태가 반복됐고, 입력할 때 폼 바깥의 상위 UI까지 다시 렌더되는 범위도 커졌습니다.

폼 상태를 React Hook Form으로 이전하고 `zodResolver`를 연결해 상태 관리와 유효성 검사 기준을 모았습니다. 일반 HTML 입력은 `register` 기반 비제어 방식으로 연결하고, 별점과 스위치 같은 커스텀 입력은 `Controller`를 사용했습니다. 상품 이미지처럼 반복되는 필드는 `useFieldArray`, 선택한 색상처럼 파생 UI에 필요한 값은 `useWatch`로 다뤘으며, 여러 하위 컴포넌트가 필드를 나눠 렌더링하는 배송지 폼에는 `FormProvider`와 `useFormContext`를 적용했습니다.

Zod schema에서 폼 값 타입을 추론하도록 하면서 타입과 검증 규칙의 불일치를 줄였고, 필드 단위 구독을 통해 입력과 관계없는 목록이나 섹션의 렌더링도 줄일 수 있었습니다. React Hook Form으로 이전한 뒤에는 필수 입력, 전화번호 정규화, 동적 필드와 custom 입력의 제출 흐름을 사용자 관점의 테스트로 확인했습니다.

프론트엔드 검증은 빠른 사용자 피드백을 위한 1차 검증이고, 신뢰할 수 없는 route param, query string, request body를 차단하는 최종 검증은 계속 Next.js Route Handler에서 수행합니다. 폼 schema를 사용한다고 해서 서버 검증을 대체할 수 없으며, 두 경계의 목적을 유지하면서 관련 schema를 각 도메인 가까이에 두는 기준을 세웠습니다.

## 다크모드 FOUC 처리

다크모드를 적용하면서 새로고침 직후 저장된 테마가 반영되기 전까지 기본 테마가 잠깐 보이는 문제가 있었습니다.

테마 값은 `localStorage`에 저장되어 있어 서버가 초기 렌더링 시점에 사용자의 테마를 알 수 없었습니다. 이를 줄이기 위해 root layout의 `<head>`에 인라인 테마 초기화 스크립트를 넣고, hydration 전에 `<html>` 클래스가 먼저 적용되도록 처리했습니다.

`dangerouslySetInnerHTML`은 사용자 입력이나 외부 데이터를 그대로 넣으면 XSS 위험이 있지만, 이 경우에는 제가 작성한 고정 스크립트만 삽입하고 사용자 입력을 문자열로 합성하지 않기 때문에 위험이 낮다고 판단했습니다.
