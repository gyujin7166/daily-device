# Daily Device 회고

프로젝트를 진행하며 고민했던 구조, 데이터 패칭, 렌더링 전략에 대한 기록입니다.

## FSD 레이어 경계

FSD 구조를 적용하면서 `entities`와 `features`의 경계가 가장 모호했습니다.

여러 화면에서 공유되는 도메인 타입, API, query key, mutation hook은 `entities`에 두고, 상품 상세나 체크아웃처럼 사용자 액션과 화면 상태가 결합된 흐름은 `features`로 분리했습니다.

다만 장바구니와 찜하기처럼 mutation 자체가 액션 UI와 강하게 묶인 기능은 경계가 특히 모호했습니다. 이 경우 mutation과 캐시 갱신 로직은 `entities`에 두고, 버튼 클릭 흐름과 화면별 조합은 `features`에서 가져다 쓰는 방식으로 정리했습니다.

이 기준을 세운 뒤에는 공유 도메인 로직과 화면별 사용자 흐름을 분리하기 수월해졌고, 특정 페이지에서만 필요한 UI 상태가 도메인 레이어에 섞이는 것을 줄일 수 있었습니다.

## TanStack Query 캐시 무효화와 낙관적 업데이트

mutation 성공 후 어떤 query를 무효화할지 기준이 필요했습니다.

장바구니, 찜하기, 배송지, 상품평처럼 여러 화면의 데이터가 함께 바뀌는 경우 관련 query key를 명확히 분리하고, 성공 시점에 `invalidateQueries`로 캐시를 서버 상태와 맞췄습니다.

장바구니 수량 조절·삭제와 찜하기는 여기에 낙관적 업데이트를 더해 응답성을 유지했습니다. 낙관적 업데이트가 실패할 경우를 대비해 `onMutate`에서 변경 전 캐시를 스냅샷으로 저장하고, `onError`에서 롤백하는 방식으로 처리했습니다.

또한 빠른 연속 클릭이나 진행 중인 백그라운드 refetch와의 경합을 줄이기 위해, 스냅샷을 저장하기 전 `cancelQueries`로 해당 query의 진행 중인 요청을 먼저 취소하도록 했습니다.

## `useSuspenseQuery`와 `useQuery` 사용 기준

서버 페이지네이션이 있는 주문 목록에 `useSuspenseQuery`를 쓰자, 캐시되지 않은 페이지로 이동할 때 Suspense fallback이 발생하며 컴포넌트가 재마운트되고 페이지 상태가 초기화되는 문제가 있었습니다.

반면 전체 데이터를 한 번에 가져와 클라이언트에서 slice 하는 배송지 화면은 query key가 바뀌지 않아 같은 문제가 발생하지 않았습니다.

이후 마운트 시 반드시 필요한 초기 데이터나 독립적인 섹션에는 `useSuspenseQuery`를 사용하고, 페이지네이션/정렬/필터처럼 사용자 조작으로 요청 파라미터가 바뀌는 화면에는 `useQuery`와 `placeholderData`를 사용하는 기준을 세웠습니다.

이를 통해 페이지 이동 중에도 기존 화면을 유지하면서 부분 로딩 상태를 표시할 수 있었고, Suspense fallback으로 인한 상태 초기화를 줄일 수 있었습니다.

## 상품 상세 페이지의 prefetch와 hydration

상품 상세 페이지에서는 서버에서 필요한 데이터를 미리 가져온 뒤, TanStack Query의 `HydrationBoundary`를 통해 클라이언트 캐시에 전달했습니다.

이를 통해 클라이언트 컴포넌트에서도 같은 query key로 상품 정보, 이미지, 상품평, 추천 상품 데이터를 재사용할 수 있었고, 초기 진입 후 불필요한 중복 요청을 줄일 수 있었습니다.

또한 찜 여부나 장바구니 상태처럼 사용자에 따라 달라지는 데이터는 클라이언트에서 별도로 조회하도록 분리했습니다.

## 정적/동적 렌더링 경계와 `auth()` 위치

처음에는 공통 쇼핑 레이아웃에서 장바구니와 위시리스트 데이터를 함께 준비하려 했지만, 이 과정에서 `auth()` 호출이 공개 상품 페이지의 정적 최적화를 막는다는 점을 확인했습니다.

`auth()`를 레이아웃에서 제거하고 로그인 상태가 필요한 부분만 `useSession()`을 사용하는 클라이언트 컴포넌트로 분리하자, 상품/카테고리 페이지가 정적 또는 ISR로 전환됐습니다.

반면 체크아웃, 마이페이지, 관리자 페이지, API Route처럼 사용자별 권한과 요청 시점의 데이터가 핵심인 영역은 그대로 동적으로 처리했습니다.

이 경험을 통해 한 페이지 안에서도 사용자와 무관한 공통 데이터와 로그인 사용자별 상태를 분리하는 것이 중요하다는 점을 체감했습니다.

## GitHub Actions 정적 생성 병목 개선

Playwright E2E에서 production build를 실행할 때 상품과 카테고리의 정적 경로 391개가 원격 TiDB를 조회하면서 전체 작업에 약 12분이 걸렸습니다. DB 연결 안정성을 위해 정적 생성 동시성을 낮춘 상태라 timeout을 늘리거나 병렬성을 무리하게 높이는 방식만으로는 해결하기 어려웠습니다.

GitHub Actions의 `E2E_BUILD` 환경에서는 E2E가 실제로 사용하는 `mice` 카테고리와 `aster-mouse-mini` 상품만 사전 생성하고, 로컬 및 Vercel build는 기존처럼 전체 경로를 생성하도록 분리했습니다. 그 결과 E2E build의 정적 생성 경로가 65개로 줄었고 전체 workflow 시간도 약 12분에서 3분대로 단축됐습니다.

이 과정에서 캐시나 timeout을 먼저 조정하기보다 각 단계의 실행 시간을 확인하고, CI의 검증 목적을 유지하는 범위에서 불필요한 작업량을 줄이는 것이 더 효과적이라는 점을 확인했습니다.

## 입력값 검증 위치에 대한 고민

현재 프로젝트에서는 `zod`를 주로 Next.js Route Handler에서 요청값을 검증하는 데 사용했습니다. route param, query string, request body처럼 서버로 들어오는 값은 잘못되거나 조작될 수 있기 때문에 API 진입 지점에서 schema 기반으로 확인했습니다.

프론트엔드 폼에서는 전화번호, 우편번호, 필수 입력값처럼 비교적 단순한 규칙을 정규표현식과 유틸 함수로 검증해, 사용자가 입력 중에 바로 오류를 확인할 수 있도록 했습니다.

이 과정을 통해 프론트엔드 검증은 사용자 피드백을 위한 1차 검증이고, 백엔드 검증은 잘못된 요청이 실제 데이터 처리로 이어지지 않도록 막기 위한 최종 검증이라는 점을 구분하게 되었습니다.

입력 폼이 더 복잡해진다면 `zod`와 form 라이브러리를 함께 사용해 검증 규칙을 더 일관되게 관리할 수 있을 것 같습니다.

## 다크모드 FOUC 처리

다크모드를 적용하면서 새로고침 직후 저장된 테마가 반영되기 전까지 기본 테마가 잠깐 보이는 문제가 있었습니다.

테마 값은 `localStorage`에 저장되어 있어 서버가 초기 렌더링 시점에 사용자의 테마를 알 수 없었습니다. 이를 줄이기 위해 root layout의 `<head>`에 인라인 테마 초기화 스크립트를 넣고, hydration 전에 `<html>` 클래스가 먼저 적용되도록 처리했습니다.

`dangerouslySetInnerHTML`은 사용자 입력이나 외부 데이터를 그대로 넣으면 XSS 위험이 있지만, 이 경우에는 제가 작성한 고정 스크립트만 삽입하고 사용자 입력을 문자열로 합성하지 않기 때문에 위험이 낮다고 판단했습니다.
